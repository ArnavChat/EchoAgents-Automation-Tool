# EchoAgents Environment Example
# Copy this file to `.env` and fill in real values. Never commit secrets like real passwords or API keys.

# ===========================================================================
# Core Database
# ===========================================================================
# Full SQLAlchemy URL (PostgreSQL example). Replace username, password, host.
DATABASE_URL=postgresql://echoagent:echoagents@localhost:5432/echoagents

# ===========================================================================
# Google Calendar (optional)
# Provide relative or absolute paths. If credentials not present, calendar features are disabled gracefully.
GOOGLE_CREDENTIALS_PATH=google/credentials.json
GOOGLE_TOKEN_PATH=google/token.json
# Set to 1 to use an in-memory dummy calendar when real creds absent (dev only)
DUMMY_CALENDAR=0

# ===========================================================================
# Localization / Timezone
TIMEZONE=Asia/Kolkata

# ===========================================================================
# Service Ports (change if ports conflict)
TIMELINE_PORT=8000
MSG_PROXY_PORT=8001
ORCHESTRATOR_PORT=8002
VOICE_AGENT_PORT=8003
COMM_AGENT_PORT=9004
INSIGHTS_AGENT_PORT=9005
FRONTEND_DEV_PORT=5173

# ===========================================================================
# Service URLs Strategy
# You may either:
#   (A) Set full URLs (TIMELINE_URL, MSG_PROXY_URL, ORCHESTRATOR_URL, VOICE_AGENT_URL)
# OR
#   (B) Set base URL + PATH pieces (e.g. TIMELINE_URL_BASE + TIMELINE_EVENTS_PATH)
# If both are set, the code prefers the fully qualified TIMELINE_URL form.

# Option A: Full base URLs (no trailing slash recommended)
TIMELINE_URL=http://localhost:${TIMELINE_PORT}
MSG_PROXY_URL=http://localhost:${MSG_PROXY_PORT}
ORCHESTRATOR_URL=http://localhost:${ORCHESTRATOR_PORT}
VOICE_AGENT_URL=http://localhost:${VOICE_AGENT_PORT}
COMM_AGENT_URL=http://localhost:${COMM_AGENT_PORT}
INSIGHTS_AGENT_URL=http://localhost:${INSIGHTS_AGENT_PORT}

# Option B: Base + path fragments (uncomment to customize paths)
# TIMELINE_URL_BASE=http://localhost:${TIMELINE_PORT}
# TIMELINE_EVENTS_PATH=/timeline/events
# ORCHESTRATOR_URL_BASE=http://localhost:${ORCHESTRATOR_PORT}
# ORCHESTRATOR_HANDLE_PATH=/handle-event
# MSG_PROXY_BASE_URL=http://localhost:${MSG_PROXY_PORT}
# MSG_PROXY_VOICE_PATH=/webhook/voice

# Explicit webhook override (takes precedence for voice agent -> msg-proxy)
# MSG_PROXY_WEBHOOK_URL=http://localhost:${MSG_PROXY_PORT}/webhook/voice

# CORS allowed origins (comma separated) for voice-agent & others
ALLOWED_ORIGINS=http://localhost:${FRONTEND_DEV_PORT},http://127.0.0.1:${FRONTEND_DEV_PORT}

# ===========================================================================
# Paths (relative to project root)
SCRIPTS_DIR=CalenderHandlerModule/scripts
SAMPLE_AUDIO=sample_audio/input.wav

# ===========================================================================
# Email SMTP (required for email send feature)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=you@example.com
EMAIL_PASSWORD=REPLACE_ME   # <-- put an app password or real secret here (never commit!)
EMAIL_FROM=you@example.com
EMAIL_USE_TLS=1

# ===========================================================================
# Frontend Build-Time injections (Webpack DefinePlugin picks these up if set before build)
# VOICE_AGENT_URL= (already defined above; can override if building frontend separately)
# MSG_PROXY_URL=
# ORCHESTRATOR_URL=

# ===========================================================================
# Logging / Misc
# (Add LOG_LEVEL or similar if needed later)

# End of file
